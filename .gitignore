.venv/
__pycache__/
*.pyc
.pytest_cache/
.idea/

# Batches and everything downstream are regenerated by the scripts in the README.
# The generator is seeded, so they come out the same every time. ~250 MB.
/data/
/artifacts/

# W&B staging; the runs themselves live on the server.
/wandb/

.env
.env.local
